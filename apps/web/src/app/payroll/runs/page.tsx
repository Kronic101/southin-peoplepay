import { AppShell } from '@/components/AppShell';


export default function Page() {
  return (
    <AppShell>
      <section className="card">
        <h1>Payroll Runs</h1>
        <p className="muted">Prepare, review, approve, lock, and report payroll runs.</p>
      </section>
    </AppShell>
  );
}
