import { AppShell } from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell>
      <section className="card">
        <h1>Overtime</h1>
        <p className="muted">Overtime requests, approvals, and payroll readiness.</p>
      </section>
    </AppShell>
  );
}
