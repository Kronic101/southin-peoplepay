import { AppShell } from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell>
      <section className="card">
        <h1>My Portal</h1>
        <p className="muted">Employee self-service for payslips, profile, leave, and contracts.</p>
      </section>
    </AppShell>
  );
}
