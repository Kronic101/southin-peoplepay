import { AppShell } from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell>
      <section className="card">
        <h1>Service Condition Templates</h1>
        <p className="muted">Configure allowances, benefits, leave, and payroll treatment per employee category.</p>
      </section>
    </AppShell>
  );
}
