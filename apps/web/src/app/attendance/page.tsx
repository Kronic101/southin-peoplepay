import { AppShell } from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell>
      <section className="card">
        <h1>Attendance</h1>
        <p className="muted">Capture and approve attendance for payroll use.</p>
      </section>
    </AppShell>
  );
}
