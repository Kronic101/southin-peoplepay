import { AppShell } from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell>
      <section className="card">
        <h1>Finance Dashboard</h1>
        <p className="muted">Payroll review, statutory totals, deductions, reconciliation, and bank file checks.</p>
      </section>
    </AppShell>
  );
}
