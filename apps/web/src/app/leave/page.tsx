import { AppShell } from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell>
      <section className="card">
        <h1>Leave</h1>
        <p className="muted">Leave requests, balances, approvals, and leave calendar.</p>
      </section>
    </AppShell>
  );
}
