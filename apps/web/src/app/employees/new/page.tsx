import { AppShell } from '@/components/AppShell';

export default function Page() {
  return (
    <AppShell>
      <section className="card">
        <h1>Create Employee</h1>
        <p className="muted">Capture employee master data and enable portal access.</p>
      </section>
    </AppShell>
  );
}
